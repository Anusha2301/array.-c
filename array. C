#include<stdio.h>
int namespace std;
bool checkprime(int n) 
int main() 
{
 int n,i;
 bool flag=flase;
 cout << "Enter a positive integer:";
 cun >> n;
 for(i=2;i<=n/2;++i)
 {
  if(checkprime(i))
 {
  if(checkprime(n-1)) 
 {
   cout << n <<  "=" << i;
   flag=true;
 }
 }
 }
 if(!flag) 
  cout << n << "can't be expressed:";
  return 0;
}
 bool checkprime(int n) 
 {
 int i;
 isprime=flase;
}
else
{
for(i=2; i<=n/2; ++i)
{
 if(n  % i == 0)
 { 
  isprime=flase;
  break;
}
 }
}
 return isprime;
}
 

 
 
